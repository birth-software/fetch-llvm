name: Build
on:
  push:
    branches:
      - 'main'
      - 'v[0-9]+'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: npm install
      - uses: EndBug/add-and-commit@v9
        with:
          add: dist
          message: Rebuild (automated)
